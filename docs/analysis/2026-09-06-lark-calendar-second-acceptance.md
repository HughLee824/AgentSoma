# Lark 55 分钟日程执行分析

分析对象：[预订Lark开发流程会议](thread://01a075a6-bf54-7763-abe5-309306764375?hostId=local)。用户要求在 iPhone Lark 中订明天 15:15–16:10、讨论开发流程优化事项的会议。执行基线为 `04572e0`，包含上一轮的接入诊断和缓存查询改动。核对了完整任务记录、47 条 shell 执行记录、逐次观察和动作回执，以及保留的 XCTest 日志。本轮只分析，未操作手机或修改实现。

**最终时间正确，持续 55 分钟；但本次执行的是修改既有日程，没有完成新建路径的验收。** 接入诊断已经改善，主要的新瓶颈是 Runner 产物发现与复用；观察查询仅有直接检查子树的使用证据，新增 `inspect --query` 没有被实际调用。

## 1. 时间调整成功，任务对象却发生了变化

首次观察 `o1` 已处于日程详情页，标题为“开发流程优化事项”，时间为明天 14:10–15:10。这与上一次验收留下的日程一致。agent 随后宣布将它调整为本次要求的时间以避免重复，点击 `o1:e16` 编辑，最终保存后的 `o10` 显示 `Tomorrow, Sep 7, 3:15 - 4:10 PM (GMT+8)`。

用户没有指定修改这个已有对象。仅凭同名、且时间不同，不足以认定它与本次要订的会议是同一个；App 恢复到上次页面也不是修改授权。应按原请求进入创建流程，不能将“避免重复”擅自提升为覆盖旧日程的依据。

保存前 agent 明确说将修改日程，用户随后回复了“确认”。因此最终保存有后续确认，不能描述为完全未经确认的修改；问题在于此前自主改变任务对象，以及这条执行路径无法证明新建能力。后续验收应同时核对新日程存在、原日程仍保持原状，不能只检查最终画面有目标时间。

本次实际调整如下；三个拖动均使用 `--velocity 100 --hold-duration 0.2`，起始停留为 0，屏幕点位移均为 48：

| 观察变化 | 动作 | 实际起止时间 |
| --- | --- | --- |
| o3 → o4 | 点击小时列文字 `03` | 14:10–15:10，未变化 |
| o4 → o5 | 开始小时向上拖一格 | 15:10–16:10 |
| o5 → o6 | 开始分钟向上拖一格 | 15:15–16:15 |
| o6 → o7 | 点击 Time 页内的结束时间按钮 | 15:15–16:15，切换到结束时间列 |
| o7 → o8 | 结束分钟向下拖一格 | 15:15–16:10 |
| o9 → o10 | 用户确认后保存 | 详情页为 9 月 7 日 15:15–16:10（GMT+8） |

三个实际拖动的回执参数和 XCTest 日志一致，随后观察均证明移动一格，没有超调、不移动或未知结果。结束时间被独立缩短 5 分钟，这个 55 分钟结果不是默认半小时的巧合。

同时，它也不是从默认半小时开始的测试：编辑原来 60 分钟的日程时，Lark 在改变开始时间后保留了原来的 60 分钟。调用方应核对实时起止值，不能把某次创建时的默认时长当成编辑时固定规则。本次分钟选项仍为 5 分钟一格，未覆盖任意分钟、AM/PM 边界或跨日。

## 2. CoreDevice 诊断生效，接入瓶颈转移到了 Runner 复用

设备发现阶段符合新提示：

- 15:39:13 首次沙盒内 `devices`，约 15.9 秒后返回 `coredevice_initialization_timeout`，明确提示受限执行环境也可能造成初始化超时。
- agent 没有重启服务，也没有在原环境盲重试；下一次同命令使用获准的宿主权限，15:39:43 成功返回设备清单。
- 从首次发现请求到成功约 29.3 秒；上一轮对应区间约 191 秒。本次有未重启服务的权限对照，证据比上一轮更直接，但单次前后对照仍不构成所有 CoreDevice 超时都由沙盒导致的证明。

随后进入了与设备发现无关的重复接入工作：

1. 读取 `.build/onboarding-build.json` 和 `.build/session-default.json`，将历史输出当作本次接入候选。旧 session 已失效；前者指向 9 月 5 日的 `ac4b99b9-…` Runner。
2. 同时运行的 `find .build -maxdepth 4 -name '*.xctestrun'` 没有找到产物；实际清单处于 `.build/runner/<id>/Build/Products/`，文件深度为 5。
3. 用旧产物 connect，经过约 26 秒安装/启动及握手后，返回 `runner_needs_rebuild`。
4. 按错误提示直接重建，未传 `--team`，因为工程也未配置团队而失败。
5. 多轮查找签名身份、profile 和 keychain。沙盒内 `security find-identity` 返回 0；profile 解码失败，又经管道产生解析错误。同一个 profile 在获准的宿主环境中可解码，不能把之前受限环境的结果当成本机不存在签名配置。
6. 使用现有团队重新构建成功，产物为 `b79e5af3-…`；新的 connect 约 11.3 秒成功。

从读取旧接入记录到连接成功约 186 秒；从本轮开始到连接成功约 4 分 20 秒。这个时间包含读帮助、签名排查、构建、连接及调用间隙，不能全部算作编译耗时。

**本机当时已有兼容产物 `d113e716-…`，上一轮刚用它成功执行；本次更深的 find 输出也列出了它。** 上次改动仅涉及 CLI/宿主，并不要求重建 Runner。本次所选旧产物确实不兼容，但这不等于所有本地产物都不兼容。

接口层缺口也很明确：[构建清单](/Users/hui/Somnus/Project/super-indie/AgentSoma/Sources/AgentSomaCore/RunnerBuild.swift:47)只保存路径，没有协议、Runner 源码指纹或已使用的签名参数，CLI 没有现成的可复用产物发现入口。[版本检查](/Users/hui/Somnus/Project/super-indie/AgentSoma/Sources/AgentSomaCore/XCTestBackend.swift:141)在 Runner 启动后进行，将所有能力不匹配统一提示为重新构建，没有给出实际与期望能力的差异。历史 onboarding/session 文件也不是由 CLI 维护的当前状态指针，新构建不会更新它们。

优先方向是提供明确的产物发现与复用路径：记录成功构建的路径、Runner 相关源码指纹、协议和已确认的签名参数，先选择符合条件的本地产物，再保留真实连接握手验证。不能仅按修改时间选最新文件，也不能因为 CLI/宿主代码变化就重建 Runner。缺少兼容产物时才进入构建；确实需要构建时优先复用已确认的配置，避免每个会话重新遍历个人签名资料。

## 3. 查询使用方式改善，但缓存搜索尚未完成实机验收

本次只有三次 `inspect`，在同一个 shell 调用中直接读取 `o3:e62`、`o3:e74`、`o3:e86` 的小时、分钟、AM/PM 子树。没有上一轮从整棵树 offset 0 开始的连续翻页，直接得到 48 点行距和控件 frame。这是更有效的调用方式。

但是，**`inspect --query` 调用次数为 0**。由于 Lark 一打开就在旧日程详情页，本次跳过了 More → Calendar 的入口查找。因此不能以 inspect 从 10 次减为 3 次或总时长下降，证明新增全文查询已经解决上轮的入口查找问题；任务路径本身更短。

`o2/o9` 编辑页依然标记 `source_truncated=true`，只是本次所需字段恰好处于已显示部分，未构成阻碍。新增缓存查询不补采源节点，这一边界保持不变。后续应覆盖一个目标未出现在默认文本中的真实页面，实际查询并记录匹配引用、frame、源缺失标记及后续定位结果；若缓存缺失而截图足以定位，再采用受画面守卫约束的坐标动作。

## 4. 两次无效尝试说明动作契约仍有使用摩擦

第一处是点击 `o3:e70`，其 AX role 为 `text`、label 为 `03`。动作返回 completed，但动作帧 hash 与进入选择器时一致，随后的 `o4` 起止时间也未变化。`enabled=true` 和可见文字不能证明点击它会选中该值；completed 表示输入及稳定检测完成，并非业务目标达成。agent 本次检查了后续观察，没有错误地把这次点击当成成功，应保留这一点。也不能据此全面禁止点击 text，因为其他 UI 的文字可能位于可点击容器内。

第二处发生在读取 `help swipe` 后：agent 把元素引用 `o4:e62` 与显式端点混用，得到 `invalid_coordinates / not_dispatched`。随后改为 `swipe o4 ... --protect o4:e62` 成功。这不是滚动不精确，而是参数表达方式混淆；[当前校验](/Users/hui/Somnus/Project/super-indie/AgentSoma/Sources/AgentSomaCore/DeviceAction.swift:21)正确拒绝了请求，没有输入手机。

最小改进是让错误提示指出具体冲突，并附带保留该元素为保护区域的修正形式。若以后允许“元素 + 端点”，必须明确它是目标、边界还是保护区域，不能为了容错静默丢弃元素引用。对于自定义滚轮，继续采用已验证的慢拖动和实时值核验，无需因这两次尝试重做手势引擎。

## 5. 耗时与流程问题

| 指标 | 本次记录 |
| --- | ---: |
| 首轮开始至最终完成 | 13 分 43 秒 |
| 两轮执行时长合计 | 8 分 47 秒 |
| 两轮之间等待确认 | 4 分 57 秒 |
| 非帮助 AgentSoma CLI 调用 | 34 次，含 2 次 build-runner 和失败请求 |
| observe / inspect | 10 次 / 3 次；全文 query 为 0 次 |
| 已执行设备输入 | 9 次：6 tap、3 swipe，全部 completed |
| 9 次输入的 Runner 耗时合计 | 37.870 秒 |
| 三次 swipe 的 Runner 耗时合计 | 13.645 秒；单次约 4.50–4.64 秒 |
| 10 次 observe 的命令耗时合计 | 5.853 秒 |

47 条 shell 记录的时间区间合并后约 139.4 秒。剩余时间混合了模型决策、工具调度、审批及调用间隙，不能全部归给模型或 XCTest。新旧流程不同，总时长不能用作受控性能对比。

从首次选择器观察 `o3` 到回到已填写编辑页 `o9` 约 144.5 秒。动作回执后仍需单独 observe 才有下一步的 AX 引用，“动作结果 + 后续新观察”的串行合并仍是有效优化方向，需要保留已派发/未知结果和新引用语义，不能靠少做核验省时间。本次在 Time 页直接点击结束时间成功，已经验证可以省去先 Done 回编辑页、再打开结束时间的绕行。

另有约 3 分 13 秒连续无用户可读进展说明。保存确认也仍分成第二轮。不过本次 agent 自己改变了任务对象，后续确认有了确认修改的作用；修正方向应首先是不擅自转为修改。在原创建授权内完成并核对草稿时继续保存，而不是先改变范围、再通过额外确认收尾。

最终详情核验后执行了 disconnect，日志证明正常关闭、没有强杀，观察缓存清理成功。本次未见 stale_reference、帧稳定超时、Spindump 长尾或 motion 回执不一致，无证据支持扩大超时或削弱画面守卫。

## 后续优先级与验收

| 优先级 | 方向 | 验收标准 |
| --- | --- | --- |
| P0 | 保持任务对象与操作意图 | 新建请求不因同名旧日程而改为覆盖；核对新旧日程分别符合预期 |
| P0 | Runner 产物发现、兼容性预检和复用 | 已有兼容产物时新会话无需重建、无需重新排查签名；不兼容时返回明确差异 |
| P1 | 补齐缓存查询的实机覆盖 | 实际使用 query 找到默认文本外目标，保留匹配和源缺失证据 |
| P1 | 减少动作与新观察的调用往返 | 相同业务核验下减少模型往返，unknown 和引用失效语义不退化 |
| P2 | 明确滑动参数错误及滚轮文字语义 | 参数混用获得可直接修正的提示；点击无效后不误报或重复点击 |

任务意图、确认和进展说明属于调用方行为；产物复用、能力诊断和参数提示属于 AgentSoma 接口。日程业务判断不应下沉进通用 Runner。

## 本地证据

- [原始任务记录](/Users/hui/.codex/sessions/2026/09/06/rollout-2026-09-06T15-37-37-01a075a6-bf54-7763-abe5-309306764375.jsonl:2)：第 43/52 行为设备发现对照，80 行为旧 Runner 拒绝，94 行为缺少团队的构建失败，179 行为连接成功，205 行宣布修改已有日程，267 行为参数拒绝，365 行为保存后观察。
- [XCTest 日志](/private/tmp/agentsoma-501/s8eb92636091a496bbcef9bf702b4419d/xcodebuild.log:132)：三次带速度和停留的拖动分别在 132、163、223 行；[正常清理记录](/private/tmp/agentsoma-501/s8eb92636091a496bbcef9bf702b4419d/exit.json)。
- 提取的[命令与输出](/private/tmp/agentsoma-lark-latest-analysis-20260906/commands.json)、[统计结果](/private/tmp/agentsoma-lark-latest-analysis-20260906/summary.json)、[初始详情](/private/tmp/agentsoma-lark-latest-analysis-20260906/o1.txt)、[保存前观察](/private/tmp/agentsoma-lark-latest-analysis-20260906/o9.txt)、[保存后观察](/private/tmp/agentsoma-lark-latest-analysis-20260906/o10.txt)。

本任务没有读取截图的工具调用，也没有内嵌图像；断开后原始观察目录已清理。上述界面结论来自留存的 AX 文本和动作帧 hash，不声称已经目视复核截图。完整日志保存在本地临时目录，不加入仓库。
